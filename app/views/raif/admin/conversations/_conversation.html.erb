<tr id="<%= dom_id(conversation) %>" class="sentinel-conversation">
  <td><%= link_to "##{conversation.id}", sentinel.admin_conversation_path(conversation) %></td>
  <td><small class="text-muted"><%= conversation.created_at.rfc822 %></small></td>
  <td><%= conversation.creator.try(:sentinel_display_name) || "#{conversation.creator_type} ##{conversation.creator_id}" %></td>
  <td><%= conversation.type %></td>
  <td><%= conversation.conversation_entries_count %></td>
</tr>
