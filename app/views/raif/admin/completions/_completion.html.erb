<tr id="<%= dom_id(completion) %>" class="sentinel-completion">
  <td><%= link_to "##{completion.id}", sentinel.admin_completion_path(completion) %></td>
  <td><small class="text-muted"><%= completion.created_at.rfc822 %></small></td>
  <td><%= completion.creator_type %> #<%= completion.creator_id %></td>
  <td><%= completion.llm_model_key %></td>
  <td>
    <% if completion.completed_at? %>
      <span class="badge bg-success"><%= t("sentinel.admin.common.completed") %></span>
    <% elsif completion.failed_at? %>
      <span class="badge bg-danger"><%= t("sentinel.admin.common.failed") %></span>
    <% elsif completion.started_at? %>
      <span class="badge bg-warning text-dark"><%= t("sentinel.admin.common.in_progress") %></span>
    <% else %>
      <span class="badge bg-secondary"><%= t("sentinel.admin.common.pending") %></span>
    <% end %>
  </td>
  <td><small class="text-muted"><%= truncate(completion.prompt, length: 100) %></small></td>
</tr>
